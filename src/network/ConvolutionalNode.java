package network;

import java.util.List;

public class ConvolutionalNode extends AbstractNode {
    public ConvolutionalNode(double x, double y, List<Node> previousNode) {
        super(x, y, previousNode);
    }
}
